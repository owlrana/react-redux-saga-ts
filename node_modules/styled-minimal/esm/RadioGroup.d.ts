import * as React from 'react';
import { NumberOrString, RadioGroupProps } from './types';
export declare class RadioGroup extends React.PureComponent<RadioGroupProps, {
    selectedValue: NumberOrString;
}> {
    constructor(props: RadioGroupProps);
    static defaultProps: {
        disabled: boolean;
        size: string;
        variant: string;
    };
    componentDidUpdate(prevProps: RadioGroupProps): void;
    update: (selectedValue: NumberOrString) => void;
    handleChange: ({ target }: React.ChangeEvent<HTMLInputElement>) => void;
    render(): JSX.Element | null;
}
